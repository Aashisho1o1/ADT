const request = require('supertest');
const app = require('../app');

describe('Disaster API', () => {
    test('should create new disaster', async () => {
        // ... test implementation
    });
}); 